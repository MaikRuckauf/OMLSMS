Django-mssql provides django with an interface to a SQL Server 2008 Database.
** Note version newer than 1.5 may produce error saving to decimal fields.
Installed using pip.

To install pip:

(python path)\python.exe get-pip.py

To install pip:

(python path)\scripts\pip install django-mssql==1.5