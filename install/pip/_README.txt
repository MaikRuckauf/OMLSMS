Pip is a package manager for Python.  This is used to install packages such as Django-mssql.
Django-mssql provides django with an interface to a SQL Server 2008 Database.

To install pip:

(python path)\python.exe get-pip.py

To install pip:

(python path)\scripts\pip install django-mssql